# Issue 002: Update dependency

## Problem
`requirements-demo.txt` pins `requests==2.28.2`, which should be reviewed and updated.

## Acceptance criteria
- Move to a reviewed safe version.
- Update any affected tests/docs.
- Confirm tests still pass.

## Suggested triage tier
**Agent-ready with more spec work** (pick target version and compatibility acceptance criteria).
