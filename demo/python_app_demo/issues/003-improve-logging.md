# Issue 003: Improve logging

## Problem
Current logging omits request context (for example `request_id`).

## Acceptance criteria
- Add structured/context-aware logging for checkout path.
- Keep current output readable for local demos.
- Add or update tests to cover logging behavior.

## Suggested triage tier
**Agent-ready with more spec work** (acceptance details on format and required fields needed).
